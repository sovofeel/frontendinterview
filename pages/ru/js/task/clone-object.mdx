---
title: Функция клонирования объекта
complexity: junior
category: js
lang: ru
description: Напишите функцию cloneObj, в результате которой создается новая копия объекта
type: task
tags: ['object', 'deep clone object']
---
```jsx example
() => {
  const obj = { a: 1, b: 2, c: { d: 4 } }
  const cloneObj = obj => {
    // write your code here 
    return obj
  }

  const result = cloneObj(obj)
  result.c.d = 10

  return JSON.stringify(obj.c.d === 4) // => true
}


```

```js answer
// Через JSON.parse/stringify
const cloneObj = obj => JSON.parse(JSON.stringify(obj))

// Через рекурсию
const cloneObj = (o, i, r) => {
  r = o
  if (r && typeof o == 'object') {
    r = o instanceof Array ? [] : {}
    for (i in o) if (o.hasOwnProperty(i)) r[i] = cloneObj(o[i])
  }
  return r
}
```
